# Hibernate Lazy Loading

**Lazy Loading** is a design pattern which is used to defer initialization of an object as long as it’s possible. Thus initial load time much smaller than in the other approach and Less memory consumption.

**But There is a big disadvantage**; *In some cases you need to handle lazily-initialized objects with a special care or you might end up with an exception due to delay initialization.*

The Exception 





 
